import { Component } from '@angular/core';
import { Config } from '../config';

@Component({
    selector: 'authentication-login',
    // template: `asd`
    templateUrl: `http://localhost:8081/public/views/authentication/login.html`
})
export class LoginComponent { }
